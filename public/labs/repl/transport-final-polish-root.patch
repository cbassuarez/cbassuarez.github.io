--- a/index.html
+++ b/index.html
@@ -10127,6 +10127,120 @@
       }
 
 
+    /* ------------------------------------------------------------------
+       Final transport button polish — exact requested edits only.
+       - evaluate hover flips stripe direction and uses green, not blue
+       - I/O hover is black/white and shortcut is keycap glyphs
+       - STOP shortcut is a keycap glyph
+       - separator divides the left control cluster from REFERENCE/DOCS
+    ------------------------------------------------------------------ */
+    .controls.transport-strip {
+      position: relative !important;
+    }
+
+    .controls.transport-strip .transport-cluster-divider {
+      grid-column: 5 !important;
+      grid-row: 1 / 3 !important;
+      align-self: stretch !important;
+      justify-self: start !important;
+      width: 0 !important;
+      min-width: 0 !important;
+      margin: -0.08rem 0 -0.08rem calc(-0.28rem - 2px) !important;
+      border-left: 3px solid var(--cs-line, #070707) !important;
+      box-shadow: 2px 0 0 var(--cs-yellow, #ffd400) !important;
+      pointer-events: none !important;
+      z-index: 8 !important;
+    }
+
+    .controls.transport-strip #play.transport-evaluate:hover,
+    .controls.transport-strip #play.transport-evaluate:focus-visible,
+    .controls.transport-strip #play:hover,
+    .controls.transport-strip #play:focus-visible {
+      --eval-center: var(--cs-green, #008f5a) !important;
+      background:
+        linear-gradient(var(--eval-center), var(--eval-center)) center / calc(100% - (var(--eval-guard) * 2)) 100% no-repeat,
+        repeating-linear-gradient(
+          45deg,
+          #ffffff 0 10px,
+          #ffffff 10px 20px,
+          #070707 20px 30px,
+          #070707 30px 40px
+        ) left center / var(--eval-guard) 100% no-repeat,
+        repeating-linear-gradient(
+          45deg,
+          #ffffff 0 10px,
+          #ffffff 10px 20px,
+          #070707 20px 30px,
+          #070707 30px 40px
+        ) right center / var(--eval-guard) 100% no-repeat !important;
+      box-shadow: 5px 5px 0 var(--cs-green, #008f5a), 8px 8px 0 var(--cs-line, #070707) !important;
+      outline: 2px solid var(--cs-green, #008f5a) !important;
+    }
+
+    .controls.transport-strip #input-toggle:hover,
+    .controls.transport-strip #input-toggle:focus-visible,
+    .controls.transport-strip #input-toggle[aria-expanded="true"] {
+      background: var(--cs-line, #070707) !important;
+      color: var(--cs-white, #ffffff) !important;
+      box-shadow: 5px 5px 0 rgba(7, 7, 7, 0.28) !important;
+      outline: 2px solid var(--cs-line, #070707) !important;
+      outline-offset: 2px !important;
+    }
+
+    .controls.transport-strip #input-toggle .button-shortcut,
+    .controls.transport-strip #stop .button-shortcut {
+      display: inline-flex !important;
+      align-items: center !important;
+      justify-content: center !important;
+      gap: 0.16rem !important;
+      margin: 0.12rem 0 0 !important;
+      padding: 0 !important;
+      border: 0 !important;
+      background: transparent !important;
+      box-shadow: none !important;
+      opacity: 1 !important;
+      visibility: visible !important;
+    }
+
+    .controls.transport-strip #input-toggle .button-shortcut kbd,
+    .controls.transport-strip #stop .button-shortcut kbd {
+      display: inline-grid !important;
+      place-items: center !important;
+      min-width: 1.04rem !important;
+      min-height: 0.88rem !important;
+      padding: 0 0.16rem !important;
+      border: 2px solid var(--cs-line, #070707) !important;
+      background: var(--cs-white, #ffffff) !important;
+      color: var(--cs-line, #070707) !important;
+      box-shadow: 2px 2px 0 rgba(7, 7, 7, 0.28) !important;
+      font-family: "Courier New", Courier, monospace !important;
+      font-size: 0.56rem !important;
+      font-weight: 900 !important;
+      line-height: 1 !important;
+      letter-spacing: 0.02em !important;
+      text-transform: uppercase !important;
+    }
+
+    .controls.transport-strip #input-toggle:hover .button-shortcut kbd,
+    .controls.transport-strip #input-toggle:focus-visible .button-shortcut kbd,
+    .controls.transport-strip #input-toggle[aria-expanded="true"] .button-shortcut kbd {
+      border-color: var(--cs-line, #070707) !important;
+      background: var(--cs-white, #ffffff) !important;
+      color: var(--cs-line, #070707) !important;
+      box-shadow: 2px 2px 0 var(--cs-yellow, #ffd400) !important;
+    }
+
+    .controls.transport-strip #stop .button-shortcut kbd {
+      box-shadow: 2px 2px 0 rgba(255, 255, 255, 0.28) !important;
+    }
+
+    @media (max-width: 74rem) {
+      .controls.transport-strip .transport-cluster-divider {
+        display: none !important;
+      }
+    }
+
+
 </style>
 </head>
 <body class="repl-loading" data-loading-phase="boot">
@@ -10308,7 +10422,7 @@
             title="Esc: stop all currently sounding voices"
           >
             <span class="button-main">stop</span>
-            <span class="button-shortcut">esc</span>
+            <span class="button-shortcut" aria-label="Escape"><kbd>⎋</kbd></span>
           </button>
 
           <button
@@ -10333,7 +10447,7 @@
             aria-controls="input-panel"
           >
             <span class="button-main">i/o</span>
-            <span class="button-shortcut">cmd/ctrl i</span>
+            <span class="button-shortcut" aria-label="Command or Control I"><kbd>⌘</kbd><kbd>⌃</kbd><kbd>I</kbd></span>
           </button>
 
           <span id="video-toolbar" class="transport-video-slot">
@@ -10349,6 +10463,8 @@
             </button>
           </span>
 
+          <span class="transport-cluster-divider" aria-hidden="true"></span>
+
           <button
             id="reference-toggle"
             class="transport-button transport-reference"
