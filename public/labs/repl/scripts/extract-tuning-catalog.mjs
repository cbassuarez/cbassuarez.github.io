#!/usr/bin/env node

console.error('extract-tuning-catalog.mjs is intentionally source-agnostic in this build.');
console.error('Use project-local tuning data ingestion tooling outside this runtime snapshot.');
process.exit(1);
