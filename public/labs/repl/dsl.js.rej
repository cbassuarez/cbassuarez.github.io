@@ -560,73 +560,6 @@
 
   function clamp(v, lo, hi) { return v < lo ? lo : v > hi ? hi : v; }
 
-  function gcdInt(a, b) {
-    let x = Math.abs(Math.floor(Number(a) || 0));
-    let y = Math.abs(Math.floor(Number(b) || 0));
-    while (y) {
-      const t = y;
-      y = x % y;
-      x = t;
-    }
-    return x || 1;
-  }
-
-  function normalizeRational(numerator, denominator) {
-    const n = Number(numerator);
-    const d = Number(denominator);
-    if (!Number.isFinite(n) || !Number.isFinite(d) || d === 0) return null;
-    const scale = 1000000;
-    let ni = Math.round(n * scale);
-    let di = Math.round(d * scale);
-    if (di < 0) { ni *= -1; di *= -1; }
-    const g0 = gcdInt(Math.abs(ni), Math.abs(di));
-    ni = Math.floor(ni / g0);
-    di = Math.floor(di / g0);
-    const g = gcdInt(Math.abs(ni), Math.abs(di));
-    return { numerator: Math.floor(ni / g), denominator: Math.floor(di / g) };
-  }
-
-  function addRationals(a, b) {
-    if (!a || !b) return null;
-    return normalizeRational(
-      a.numerator * b.denominator + b.numerator * a.denominator,
-      a.denominator * b.denominator,
-    );
-  }
-
-  function rationalToMeterUnits(value, meter) {
-    if (!value) return NaN;
-    const denominator = Number(meter && meter.den) > 0 ? Number(meter.den) : 4;
-    return (Number(value.numerator) / Number(value.denominator)) * denominator;
-  }
-
-  function parsePickupDurationExpression(raw, meter) {
-    const source = String(raw || '').trim().toLowerCase().replace(/\s+/g, '');
-    if (!source) return null;
-
-    let total = normalizeRational(0, 1);
-    const denominator = Number(meter && meter.den) > 0 ? Number(meter.den) : 4;
-    const terms = source.split('+').filter(Boolean);
-    if (!terms.length) return null;
-
-    for (const term of terms) {
-      let value = null;
-      const frac = term.match(/^(\d+(?:\.\d+)?|\.\d+)\/(\d+(?:\.\d+)?|\.\d+)$/);
-      if (frac) {
-        value = normalizeRational(Number(frac[1]), Number(frac[2]));
-      } else if (/^(\d+(?:\.\d+)?|\.\d+)$/.test(term)) {
-        // Bare numbers are counted in the current meter denominator unit:
-        // in 4/4, pickup 1 === pickup 1/4; in 6/8, pickup 1 === pickup 1/8.
-        value = normalizeRational(Number(term), denominator);
-      }
-      if (!value || value.numerator <= 0 || value.denominator <= 0) return null;
-      total = addRationals(total, value);
-      if (!total) return null;
-    }
-
-    return total && total.numerator > 0 ? total : null;
-  }
-
   // -------------------- slot tokenizer (paren-aware) --------------------
 
   // Tokenizes a string into a flat array of tokens where each token is
@@ -637,31 +570,17 @@
   // own characters even when adjacent to text.
   function tokenizeSlotLine(text) {
     const out = [];
-    const source = String(text || '');
     let buf = '';
     function flush() {
       if (buf.length) { out.push(buf); buf = ''; }
     }
-    for (let i = 0; i < source.length; i++) {
-      const ch = source[i];
-      if (ch === '(' || ch === ')' || ch === '|') {
+    for (let i = 0; i < text.length; i++) {
+      const ch = text[i];
+      if (ch === '(' || ch === ')') {
         flush();
         out.push(ch);
         continue;
       }
-      if (ch === '?') {
-        flush();
-        const next = source[i + 1] || '';
-        if (next && !/\s/.test(next) && next !== ')' && next !== '|') {
-          let j = i + 1;
-          while (j < source.length && !/\s/.test(source[j]) && !['(', ')', '|'].includes(source[j])) j += 1;
-          out.push(source.slice(i, j));
-          i = j - 1;
-        } else {
-          out.push('?');
-        }
-        continue;
-      }
       if (/\s/.test(ch)) { flush(); continue; }
       buf += ch;
     }
@@ -674,7 +593,7 @@
   // SlotNode:
   //   { kind: 'leaf', token: { kind: 'note'|'rest'|'sustain'|'sample', value } }
   //   { kind: 'group', children: [SlotNode] }
-  function parseSlotStream(tokens, voice, lineNumber, pickupContext) {
+  function parseSlotStream(tokens, voice, lineNumber) {
     const errors = [];
 
       function classifyLeaf(tok) {
@@ -801,29 +720,6 @@
     const slots = [];
     const barSlotCounts = [];
     let currentBarSlots = 0;
-    const pickupActive = Boolean(pickupContext && Number(pickupContext.beats) > 0);
-    const pickupDuration = pickupContext && pickupContext.duration ? pickupContext.duration : null;
-    let sawPickupGap = false;
-
-    function pickupGapNode(role, attachedToPrevious) {
-      return {
-        kind: 'pickup-gap',
-        raw: '?',
-        role,
-        structural: true,
-        playable: false,
-        attachedToPrevious: Boolean(attachedToPrevious),
-        token: {
-          kind: 'pickup-gap',
-          raw: '?',
-          role,
-          structural: true,
-          playable: false,
-          attachedToPrevious: Boolean(attachedToPrevious),
-          duration: pickupDuration ? { ...pickupDuration } : null,
-        },
-      };
-    }
 
     function pushSlot(node) {
       slots.push(node);
@@ -831,7 +727,7 @@
     }
 
     function collectLeafRefs(node, out) {
-      if (!node || node.kind === 'pickup-gap') return;
+      if (!node) return;
       if (node.kind === 'leaf') {
         out.push(node);
         return;
@@ -976,12 +872,6 @@
           errors.push({ line: lineNumber, message: `unexpected '|' inside (...) group — bar lines belong only between top-level slots` });
           continue;
         }
-        if (t === '?' || /^\?/.test(String(t || ''))) {
-          pos++;
-          errors.push({ line: lineNumber, message: `pickup bookend '?' cannot appear inside (...) — write it only at the opening edge or terminal edge of a pickup phrase` });
-          children.push({ kind: 'leaf', token: { kind: 'rest', value: null, raw: '.' } });
-          continue;
-        }
         const leaf = classifyLeaf(t);
           if (!leaf) {
             const gateError = voiceGateError(t);
@@ -1037,37 +927,6 @@
         pos++;
         continue;
       }
-      if (t === '?') {
-        const prev = tokens[pos - 1] || '';
-        const next = tokens[pos + 1] || '';
-        const beforeFirstBar = barSlotCounts.length === 0;
-        const atTerminalEdge = pos === tokens.length - 1;
-        const attachedToPrevious = prev === ')';
-
-        if (!pickupActive) {
-          errors.push({ line: lineNumber, message: `pickup bookend '?' requires an active pickup/anacrusis directive` });
-        } else if (prev === '?' || next === '?') {
-          errors.push({ line: lineNumber, message: `adjacent pickup bookends '? ?' are not allowed — '?' is a bookend, not a repeatable rest` });
-        } else if (beforeFirstBar && currentBarSlots === 0) {
-          pushSlot(pickupGapNode('opening', false));
-          sawPickupGap = true;
-        } else if (atTerminalEdge && currentBarSlots > 0) {
-          pushSlot(pickupGapNode('terminal', attachedToPrevious));
-          sawPickupGap = true;
-        } else if (beforeFirstBar) {
-          errors.push({ line: lineNumber, message: `opening pickup bookend '?' must appear before pickup material, e.g. piano ? G4 | ...` });
-        } else {
-          errors.push({ line: lineNumber, message: `terminal pickup bookend '?' must appear at the end of the closing phrase, e.g. ... * * (*)?` });
-        }
-        pos++;
-        continue;
-      }
-      if (/^\?/.test(String(t || ''))) {
-        errors.push({ line: lineNumber, message: `pickup bookend '?' must be its own structural token — write '? G4', not '${t}'` });
-        pushSlot({ kind: 'leaf', token: { kind: 'rest', value: null, raw: '.' } });
-        pos++;
-        continue;
-      }
       if (t === '(') {
         pos++;
         pushSlot(parseGroup());
@@ -1120,7 +979,7 @@
     finalizePitchSpans();
     const bars = barSlotCounts.length;
 
-    return { slots, bars, barSlotCounts, hasPickupGap: sawPickupGap, errors };
+    return { slots, bars, barSlotCounts, errors };
   }
 
     // -------------------- parameter resolution --------------------
@@ -2085,43 +1942,6 @@
       }
     }
 
-    function parsePickupDirective(args, lineNumber, directiveName) {
-      if (!Array.isArray(args) || args.length !== 1) {
-        return {
-          ok: false,
-          error: { line: lineNumber, message: `${directiveName || 'pickup'} needs one duration value, e.g. 'pickup 1', 'pickup 3/8', or 'pickup 1/8+1/16+1/16'` },
-        };
-      }
-
-      const raw = String(args[0] || '').trim();
-      const duration = parsePickupDurationExpression(raw, meter);
-      if (!duration) {
-        return {
-          ok: false,
-          error: { line: lineNumber, message: `${directiveName || 'pickup'} duration must be positive, e.g. 'pickup 1', 'pickup 3/8', or 'pickup 1/8+1/16+1/16'` },
-        };
-      }
-
-      const beats = rationalToMeterUnits(duration, meter);
-      if (!Number.isFinite(beats) || beats <= 0) {
-        return {
-          ok: false,
-          error: { line: lineNumber, message: `${directiveName || 'pickup'} duration could not be resolved against the current meter` },
-        };
-      }
-
-      return {
-        ok: true,
-        value: {
-          beats,
-          raw,
-          line: lineNumber,
-          directive: directiveName || 'pickup',
-          duration,
-        },
-      };
-    }
-
     function parseEvaluateFlag(args, lineNumber, requireMode, sourceName) {
       if (!Array.isArray(args) || args.length === 0) {
         if (requireMode) {
@@ -2484,7 +2297,7 @@
 
         if (slotTokens.length > 0) {
           const voiceName = videoSource === 'gen' ? 'video-gen' : 'video';
-          const result = parseSlotStream(slotTokens, voiceName, lineNumber, pickup);
+          const result = parseSlotStream(slotTokens, voiceName, lineNumber);
           if (result.errors.length) {
             for (const e of result.errors) errors.push(e);
           }
@@ -2567,7 +2380,7 @@
           errors.push({ line: lineNumber, message: `voice '${head}' has no slots — add some notes or rests after it` });
           continue;
         }
-        const result = parseSlotStream(slotTokens, voiceName, lineNumber, pickup);
+        const result = parseSlotStream(slotTokens, voiceName, lineNumber);
         if (result.errors.length) {
           for (const e of result.errors) errors.push(e);
         }
@@ -3017,41 +2830,6 @@
       return { ok: false, errors };
     }
 
-    const meterUnits = Number.isFinite(Number(meter && meter.num)) && Number(meter.num) > 0 ? Number(meter.num) : 4;
-    const pickupBeats = pickup && Number.isFinite(Number(pickup.beats)) && Number(pickup.beats) > 0
-      ? Number(pickup.beats)
-      : 0;
-    const pickupValue = pickupBeats > 0
-      ? {
-          beats: pickupBeats,
-          raw: pickup.raw || String(pickupBeats),
-          line: pickup.line || null,
-          directive: pickup.directive || 'pickup',
-          duration: pickup.duration ? { ...pickup.duration } : normalizeRational(pickupBeats, meter.den || 4),
-          phaseOffset: ((meterUnits - (pickupBeats % meterUnits)) % meterUnits),
-        }
-      : { beats: 0, raw: '', line: null, directive: 'pickup', duration: null, phaseOffset: 0 };
-
-    if (pickupBeats > 0 && pickupBeats >= meterUnits) {
-      warnings.push({
-        line: pickup && pickup.line ? pickup.line : 1,
-        message: `pickup ${pickupValue.raw} is at least one full ${meter.num}/${meter.den} bar; it will still be treated as a metrical phase offset`,
-      });
-    }
-
-    if (pickupBeats > 0) {
-      const hasBookend = blocks.some((block) => {
-        const slots = block && Array.isArray(block.slots) ? block.slots : [];
-        return slots.some((slot) => slot && slot.kind === 'pickup-gap');
-      });
-      if (!hasBookend) {
-        warnings.push({
-          line: pickup && pickup.line ? pickup.line : 1,
-          message: `pickup ${pickupValue.raw} is active, but no '?' bookend was found; use '?' to mark omitted anacrusis space`,
-        });
-      }
-    }
-
     // Pre-resolve pitched sustain leaves (~) to the immediately-preceding note
     // in DFS order. Sample/noise/pulse sustains remain explicit sustain leaves so the
     // scheduler/editor can commit and highlight them distinctly from rests.
@@ -3226,7 +3001,6 @@
       }
 
       function visit(node) {
-        if (!node || node.kind === 'pickup-gap') return;
         if (node.kind === 'leaf') {
           const tok = node.token;
 
