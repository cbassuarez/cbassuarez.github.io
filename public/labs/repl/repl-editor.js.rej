@@ -317,7 +317,6 @@
     if (stream.match(/^\*/)) return 'operator';
 
     if (stream.match(/^\|/)) return 'separator';
-    if (stream.match(/^\?/)) return 'operator';
     if (stream.match(/^[()]/)) return 'bracket';
     if (stream.match(/^[~_]/)) return 'operator';
     if (stream.match(/^;/)) return 'operator';
@@ -606,7 +605,6 @@
     if (/^[()]/.test(raw)) return 'cs-token cs-bracket';
     if (/^(?:<<|>>|<|>)(?:[A-G][b#]?-?\d+|[A-G][b#]?\*!?|\*!\d|\*\d|\*!|\*|[0-8]\*)$/.test(raw)) return 'cs-token cs-pitch';
     if (/^[A-G][b#]?%$/.test(raw)) return 'cs-token cs-pitch';
-    if (raw === '?') return 'cs-token cs-pickup-gap';
     if (/^(?:\*|\*!|\*~|\*&\d+!?|\*!\d+|\*\d+|~|_|\||;|\.|-)$/.test(raw)) return 'cs-token cs-operator';
     if (/^-?\d+\/\d+$/.test(raw) || /^-?\d*\.\d+$/.test(raw) || /^-?\d+$/.test(raw) || /^\d+(?:ms|s)$/.test(raw) || /^pi(?:\/\d+)?$/.test(raw)) return 'cs-token cs-number';
     if (/^[A-G][b#]?-?\d+$/.test(raw) || /^[A-G][b#]?\*!?$/.test(raw)) return 'cs-token cs-pitch';
@@ -626,7 +624,7 @@
     })();
 
     const codePart = commentAt >= 0 ? lineText.slice(0, commentAt) : lineText;
-    const tokenRe = /[^\s()|;?]+|[()|;?]/g;
+    const tokenRe = /[^\s()|;]+|[()|;]/g;
     let m;
     let first = true;
     while ((m = tokenRe.exec(codePart))) {
@@ -670,7 +668,7 @@
   }
 
   function isLeafStructuralToken(value) {
-    return /^(?:\(|\)|\||;|\?)$/.test(String(value || '').trim());
+    return /^(?:\(|\)|\||;)$/.test(String(value || '').trim());
   }
 
   function isLeafTokenRange(range) {
@@ -722,7 +720,7 @@
   }
 
   function isLeafStructuralToken(value) {
-    return /^(?:\(|\)|\||;|\?)$/.test(String(value || '').trim());
+    return /^(?:\(|\)|\||;)$/.test(String(value || '').trim());
   }
 
   function buildLeafSourceTree(lineText) {
@@ -1737,137 +1735,6 @@
   // theme — Cybernetic Score: white Memphis/MTA instrument-score surface.
   // ============================================================================
 
-  function docHasPickupDirective(doc) {
-    if (!doc) return false;
-    const limit = Math.min(doc.lines || 0, 96);
-    for (let lineNumber = 1; lineNumber <= limit; lineNumber += 1) {
-      const line = doc.line(lineNumber).text.trim().toLowerCase();
-      if (/^(pickup|anacrusis)\b/.test(line)) return true;
-    }
-    return false;
-  }
-
-  function scoreRulerLineHead(text) {
-    const m = String(text || '').match(/^(\s*)(\S+)(\s*)/);
-    if (!m) return null;
-    return {
-      head: m[2],
-      headTo: (m[1] || '').length + (m[2] || '').length,
-      restFrom: (m[1] || '').length + (m[2] || '').length + (m[3] || '').length,
-    };
-  }
-
-  function firstScoreVoiceLine(doc) {
-    if (!doc) return null;
-    for (let lineNumber = 1; lineNumber <= doc.lines; lineNumber += 1) {
-      const line = doc.line(lineNumber);
-      const info = scoreRulerLineHead(line.text);
-      if (!info || !HEAD_VOICE.has(String(info.head || '').toLowerCase())) continue;
-      const body = line.text.slice(info.restFrom).trim();
-      if (body) return line;
-    }
-    return null;
-  }
-
-  function scoreRulerMeasureChPx(view) {
-    if (view && Number.isFinite(Number(view.defaultCharacterWidth)) && Number(view.defaultCharacterWidth) > 0) {
-      return Number(view.defaultCharacterWidth);
-    }
-    return 8.4;
-  }
-
-  function scoreRulerLineTextLeftPx(view) {
-    const lineEl = view && view.dom ? view.dom.querySelector('.cm-line') : null;
-    if (!lineEl) return 0;
-    try {
-      const style = root.getComputedStyle(lineEl);
-      return Number.parseFloat(style.paddingLeft) || 0;
-    } catch (_) {
-      return 0;
-    }
-  }
-
-  function scoreRulerSync(view, layer) {
-    if (!view || !layer || !view.state || !view.state.doc) return;
-    const doc = view.state.doc;
-    const firstLine = firstScoreVoiceLine(doc);
-    const hasPickup = docHasPickupDirective(doc);
-    layer.replaceChildren();
-    if (!firstLine) return;
-
-    const chPx = scoreRulerMeasureChPx(view);
-    const textLeft = scoreRulerLineTextLeftPx(view);
-    let top = 0;
-    try {
-      const block = view.lineBlockAt(firstLine.from);
-      if (block && Number.isFinite(Number(block.top))) top = Number(block.top);
-    } catch (_) {}
-
-    const info = scoreRulerLineHead(firstLine.text);
-    if (!info) return;
-    const originX = textLeft + Math.max(info.restFrom, info.headTo + 1) * chPx;
-    const height = Math.max(view.contentHeight || 0, view.scrollDOM ? view.scrollDOM.scrollHeight : 0, 320);
-
-    function addRule(className, x) {
-      const el = document.createElement('div');
-      el.className = className;
-      el.style.left = `${Math.max(0, x).toFixed(2)}px`;
-      el.style.top = `${Math.max(0, top).toFixed(2)}px`;
-      el.style.height = `${Math.max(0, height - top).toFixed(2)}px`;
-      layer.appendChild(el);
-      return el;
-    }
-
-    addRule('cm-score-ruler-origin', originX);
-
-    if (hasPickup) {
-      const barIndex = firstLine.text.indexOf('|', info.restFrom);
-      if (barIndex >= 0) {
-        const downbeatX = textLeft + barIndex * chPx;
-        const span = document.createElement('div');
-        span.className = 'cm-score-ruler-pickup-span';
-        span.style.left = `${Math.min(originX, downbeatX).toFixed(2)}px`;
-        span.style.top = `${Math.max(0, top).toFixed(2)}px`;
-        span.style.width = `${Math.max(0, Math.abs(downbeatX - originX)).toFixed(2)}px`;
-        span.style.height = `${Math.max(0, height - top).toFixed(2)}px`;
-        layer.appendChild(span);
-        addRule('cm-score-ruler-downbeat', downbeatX);
-      }
-    }
-  }
-
-  const scoreRulerPlugin = ViewPlugin.fromClass(class {
-    constructor(view) {
-      this.view = view;
-      this.frame = null;
-      this.layer = document.createElement('div');
-      this.layer.className = 'cm-score-ruler-layer';
-      this.layer.setAttribute('aria-hidden', 'true');
-      view.contentDOM.appendChild(this.layer);
-      this.schedule();
-    }
-
-    schedule() {
-      if (this.frame != null) return;
-      this.frame = root.requestAnimationFrame(() => {
-        this.frame = null;
-        scoreRulerSync(this.view, this.layer);
-      });
-    }
-
-    update(update) {
-      if (update.docChanged || update.viewportChanged || update.geometryChanged || update.selectionSet) {
-        this.view = update.view;
-        this.schedule();
-      }
-    }
-
-    destroy() {
-      if (this.frame != null) root.cancelAnimationFrame(this.frame);
-      if (this.layer && this.layer.parentNode) this.layer.parentNode.removeChild(this.layer);
-    }
-  });
-
   const replTheme = EditorView.theme({
     '&': {
       position: 'relative',
@@ -1909,34 +1776,6 @@
       lineHeight: '1.62',
       color: '#070707',
       counterReset: 'cs-line',
-      position: 'relative',
-    },
-    '.cm-score-ruler-layer': {
-      position: 'absolute',
-      left: '0',
-      top: '0',
-      right: '0',
-      bottom: '0',
-      pointerEvents: 'none',
-      zIndex: '0',
-      overflow: 'visible',
-    },
-    '.cm-score-ruler-origin, .cm-score-ruler-downbeat': {
-      position: 'absolute',
-      width: '0',
-      borderLeft: '2px solid rgba(7,7,7,0.26)',
-      boxShadow: '1px 0 0 rgba(255,212,0,0.18)',
-      pointerEvents: 'none',
-    },
-    '.cm-score-ruler-downbeat': {
-      borderLeftColor: 'rgba(7,7,7,0.42)',
-      boxShadow: '2px 0 0 rgba(0,87,255,0.12)',
-    },
-    '.cm-score-ruler-pickup-span': {
-      position: 'absolute',
-      backgroundImage: 'repeating-linear-gradient(135deg, rgba(7,7,7,0.025) 0, rgba(7,7,7,0.025) 2px, transparent 2px, transparent 7px)',
-      backgroundColor: 'rgba(255,212,0,0.035)',
-      pointerEvents: 'none',
     },
     '.cm-line': {
       position: 'relative',
@@ -2287,13 +2126,6 @@
       color: '#5c20df',
       fontWeight: '900',
     },
-    '.cs-pickup-gap': {
-      color: 'rgba(7,7,7,0.62)',
-      fontWeight: '900',
-      backgroundImage: 'repeating-linear-gradient(135deg, rgba(7,7,7,0.12) 0, rgba(7,7,7,0.12) 2px, transparent 2px, transparent 6px)',
-      boxShadow: 'inset 0 -2px 0 rgba(255,212,0,0.42)',
-      borderRadius: '2px',
-    },
     '.cs-number': {
       color: '#070707',
       fontWeight: '800',
@@ -3170,15 +3002,6 @@
         ], 'directive');
         return { from, options: opts };
       }
-      if (here.directive === 'pickup' || here.directive === 'anacrusis') {
-        const opts = withCompletionCategoryList([
-          { label: '1', type: 'constant', detail: 'one current meter unit' },
-          { label: '3/8', type: 'constant', detail: 'three-eighth pickup duration' },
-          { label: '5/16', type: 'constant', detail: 'five-sixteenth pickup duration' },
-          { label: '1/8+1/16+1/16', type: 'constant', detail: 'mixed pickup duration' },
-        ], 'directive');
-        return { from, options: opts, validFor: /^[0-9/+._-]*$/ };
-      }
       if (here.directive === 'meter') {
         const opts = withCompletionCategoryList([
           { label: '4/4', type: 'constant' },
@@ -3312,8 +3135,6 @@
           { label: 'A*', type: 'keyword', detail: 'random A octave' },
           { label: '~', type: 'keyword', detail: 'sustain previous' },
           { label: '.', type: 'keyword', detail: 'rest' },
-          { label: '?', type: 'keyword', detail: 'pickup bookend / omitted anacrusis span' },
-          { label: '(*)?', type: 'keyword', detail: 'terminal pickup bookend after a subdivided cell' },
           { label: '|', type: 'keyword', detail: 'bar' },
         ];
         return { from, options: withCompletionCategoryList(opts, 'voice') };
@@ -3324,8 +3145,6 @@
           { label: '*!', type: 'keyword', detail: here.voice === 'pulse' ? 'frozen pulse hit' : 'frozen noise hit' },
           { label: '~', type: 'keyword', detail: here.voice === 'pulse' ? 'held pulse cell' : 'held noise texture' },
           { label: '.', type: 'keyword', detail: 'rest' },
-          { label: '?', type: 'keyword', detail: 'pickup bookend / omitted anacrusis span' },
-          { label: '(*)?', type: 'keyword', detail: 'terminal pickup bookend after a subdivided cell' },
           { label: '|', type: 'keyword', detail: 'bar' },
         ];
         return { from, options: withCompletionCategoryList(opts, 'voice') };
@@ -3350,8 +3169,6 @@
           { label: '*!', type: 'keyword', detail: 'frozen kit-pool pick for this leaf' },
           { label: '~', type: 'keyword', detail: 'hold cell' },
           { label: '.', type: 'keyword', detail: 'rest' },
-          { label: '?', type: 'keyword', detail: 'pickup bookend / omitted anacrusis span' },
-          { label: '(*)?', type: 'keyword', detail: 'terminal pickup bookend after a subdivided cell' },
           { label: '|', type: 'keyword', detail: 'bar' },
         ];
         return { from, options: withCompletionCategoryList(opts, 'voice') };
@@ -3367,8 +3184,6 @@
           { label: '*!', type: 'keyword', detail: 'frozen visual hit token' },
           { label: '~', type: 'keyword', detail: 'hold cell' },
           { label: '.', type: 'keyword', detail: 'rest' },
-          { label: '?', type: 'keyword', detail: 'pickup bookend / omitted anacrusis span' },
-          { label: '(*)?', type: 'keyword', detail: 'terminal pickup bookend after a subdivided cell' },
           { label: '|', type: 'keyword', detail: 'bar' },
         ];
         return { from, options: withCompletionCategoryList(opts, 'voice'), validFor: /^[A-Za-z0-9_.!*~-]*$/ };
@@ -3680,7 +3495,6 @@
       replLanguageWithTags,
       syntaxHighlighting(replHighlight),
       cyberneticScorePlugin,
-      scoreRulerPlugin,
       selectionStateClassPlugin,
       selectionMaskPlugin,
       blockMuteDecorationsPlugin,
